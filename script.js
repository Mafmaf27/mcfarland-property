const menuButton=document.querySelector(".menu-toggle"),nav=document.querySelector(".main-nav");menuButton?.addEventListener("click",()=>{const open=nav.classList.toggle("open");menuButton.setAttribute("aria-expanded",String(open))});document.querySelectorAll(".main-nav a").forEach(link=>link.addEventListener("click",()=>{nav.classList.remove("open");menuButton?.setAttribute("aria-expanded","false")}));document.getElementById("year").textContent=new Date().getFullYear();const comparison=document.querySelector("[data-comparison]");if(comparison){const range=comparison.querySelector(".comparison-range"),before=comparison.querySelector("[data-before]"),handle=comparison.querySelector("[data-handle]");const update=()=>{before.style.width=`${range.value}%`;handle.style.left=`${range.value}%`};range.addEventListener("input",update);update()}const quoteForm=document.querySelector(".quote-form");quoteForm?.addEventListener("submit",event=>{if(quoteForm.action.includes("YOUR_FORM_ID")){event.preventDefault();alert("The quote form still needs your Formspree form ID. Follow the README instructions before accepting enquiries.")}});